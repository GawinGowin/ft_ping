{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; echo \"$f\" | grep -qE '\\.(c|h|cpp|hpp)$' && clang-format --dry-run --Werror \"$f\"; } 2>/dev/null || true",
            "statusMessage": "Running clang-format lint..."
          }
        ]
      }
    ]
  }
}
