{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|Update",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; echo \"$f\" | grep -qE '\\.(c|h|cpp|hpp)$' && clang-format -i \"$f\"; } 2>/dev/null || true",
            "statusMessage": "Running clang-format lint..."
          }
        ]
      }
    ]
  }
}
